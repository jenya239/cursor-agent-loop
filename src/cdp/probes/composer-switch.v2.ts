export const COMPOSER_SWITCH_V2_PROBE_ID = 'composer-switch.v2' as const;

/** v2 runs via CDP keyboard in switch-quick-open.ts, not in-page JS. */
export const COMPOSER_SWITCH_V2_NOTE = 'use switchViaQuickOpen after v1';
